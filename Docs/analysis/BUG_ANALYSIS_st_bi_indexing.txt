"""
CRITICAL BUG ANALYSIS: Model/Data Mismatch
============================================

DATE: 2026-03-26
ISSUE: Battery Project Integer UNSATISFIABLE after st_bi indexing fix
ROOT CAUSE: Model still expects st_bi=0 for padding, but data now uses 1-indexing

=== THE PROBLEM ===

MODEL ASSUMPTION (old, 0-indexed):
  st_bi can be 0 (depot/padding)  OR  1..num_stations (real stations)

DATA NOW (1-indexed):
  st_bi always >= 1 (1..num_stations)
  Padding NOT marked by st_bi=0

MODEL CONSTRAINTS affected:
  Line 108-112: Filters for st_bi[b,i] == 0 to handle padding
  Line 102: Filters for st_bi[b,i] > 0 for charging stations
  Line 121: Filters for st_bi[b,i] > 0 for overlap detection

CONSEQUENCE:
  - Padding stops (i > num_stops[b]) are now treated as REAL stations
  - Model tries to satisfy constraints for non-existent stops
  - UNSATISFIABLE

=== SOLUTION OPTIONS ===

OPTION A: Use boundary checking instead of st_bi==0
  Filter by: i <= num_stops[b] instead of st_bi[b,i] > 0
  Pro: Cleaner, more explicit, works with any indexing
  Con: Requires model restructuring

OPTION B: Keep 0-indexed data, use 1-indexing only in internal mapping
  Filter st_bi in conversion script: keep 0-indexed in DZN
  Pro: No model changes needed
  Con: Confusing (internal vs external indexing)

OPTION C: Accept UNSAT as specification issue, not just data
  Investigate if Cork instances have inherent infeasibility
  Pro: Might reveal deeper model/data issues

=== IMMEDIATE FIX (OPTION A) ===

Model changes needed in clp_model.mzn:

1. Line 102-105: Change from
   forall(b in B, i in 1..num_stops[b] where st_bi[b,i] > 0)
   to
   forall(b in B, i in 1..num_stops[b])

2. Line 108-112: Change from
   forall(b in B, i in 1..num_stops[b] where st_bi[b,i] == 0)
   to
   forall(b in B, i in (num_stops[b]+1)..max_stops)

3. Line 120-121: Change from
   forall(...where st_bi[b,i] == st_bi[d,j] /\ st_bi[b,i] > 0)
   to
   forall(...where st_bi[b,i] == st_bi[d,j] /\
                     i <= num_stops[b] /\ j <= num_stops[d])

RATIONALE:
  - num_stops[b] indicates the REAL number of stops for bus b
  - Boundaries 1..num_stops[b] = real stops
  - (num_stops[b]+1)..max_stops = padding/dummy
  - st_bi value doesn't matter for padding

=== STATUS ===

To verify:
1. Backup clp_model.mzn
2. Apply the 3 changes above
3. Test with cork-1-line20_0.dzn
4. If SAT/UNKNOWN → fix is correct
5. Update all DZN generation to assume 0-indexed → 1-indexed conversion

"""
